#include <stdio.h>
/**
 * main - start point.
 * @argc: size of argv
 * @argv: name of app
 * root-codes
 * Return: Always 0 (Success)
 */
int main(int argc __attribute__((unused)), char *argv[])
{
	printf("%s\n", argv[0]);
	return (0);
}
