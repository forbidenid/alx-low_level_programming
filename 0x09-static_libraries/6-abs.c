#include "main.h"
/**
 * _abs - computers the absolute value of an integer
 * @n: integer
 * root-codes
 * Return: abolute value of an integer
 */
int _abs(int n)
{
	if (n >= 0)
		return (n);
	else
		return (-n);
}
